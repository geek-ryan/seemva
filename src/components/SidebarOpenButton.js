import React, { Component } from 'react';
import { Form, Input, Icon, Button } from 'antd';

class SidebarOpenButton extends Component {
  render() {
    return <Icon type="right-circle-o" />;
  }
}

export default SidebarOpenButton;
