import React, { Component } from 'react';
import { Form, Input, Icon, Button } from 'antd';

class SidebarOpenButtonPC extends Component {
  render() {
    return <Icon type="right-circle-o" />;
  }
}

export default SidebarOpenButtonPC;
