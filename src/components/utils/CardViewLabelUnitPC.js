import React, { Component } from 'react';
import { Form, Input, Icon, Button } from 'antd';

class CardViewLabelUnitPC extends Component {
  render() {
    return (
      <React.Fragment>
        <Icon className={this.props.Color} type="minus" />
      </React.Fragment>
    );
  }
}

export default CardViewLabelUnitPC;
